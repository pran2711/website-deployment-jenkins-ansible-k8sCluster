- hosts: all
  become: true
  tasks: 
     - name: create new deployment
       command: kubectl delete -f /var/depl.yml
     - name: create new service
       command: kubectl delete -f /var/service.yml